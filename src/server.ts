interface User {
    name: string
    age: number
}
function saveUserToDatebase(user: User)  {
    // salvar no banco de dados


console.log(user)
}
saveUserToDatebase({
    name: 'natálya',
    age: 24
})
